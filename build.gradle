plugins {
    id 'java'
}

allprojects {
    group = "be.theking90000.mclib2"
    version = "0.0.1"

    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: "java"

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(8)
        }
    }
}